# Uses the model/training defaults in dom_vpwem.config.
# Collect trajectories with the split cover present; this variant has no public dataset.
task:
  env_id: InterceptFastCover2-VLA-v0
  dataset_dir: data_mikasa_robo/data_npz/intercept_fast_cover2_vla_v0_collision_cue5_v1
  max_episode_steps: 60

train:
  output_dir: outputs/intercept_fast_cover2
